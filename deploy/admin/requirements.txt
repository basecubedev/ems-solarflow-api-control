# Minimal runtime dependencies; no database or Docker client.
requests>=2.32,<3
zeroconf>=0.132,<1
# Read-only local MQTT topic discovery (Admin only; never publishes). Discovery
# degrades to broker-endpoint-only when this import is unavailable.
paho-mqtt>=2.1.0
# Backup / restore reuses the EMS backup core, which reads/writes encrypted
# (AEAD) archives; cryptography is required for inspect/restore of those.
cryptography
